import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.composer.donttextyourex',
appName: "Don't Text Your Ex",
  webDir: 'dist'
};

export default config;
